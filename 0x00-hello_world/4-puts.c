#include <stdio.h>

/**
 * main: calls puts function
 * Description: this program prints the string in the puts function
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programing is like building a multilingual puzzle");
	return (0);
}
